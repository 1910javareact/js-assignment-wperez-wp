/* 11. Splice Element
Define function spliceElement(someArr, index)
Splice the element at the provided index in the array.
*/  
function spliceElement(Arr, index){
 
 let arr1 = (arr.splice(index, 0, 15))
  console.log(Arr)
}
let arr = ["HELLO","GOODBY","HAPPY","INVOLVED","DETERMINED", "FOCUSED"]
spliceElement(arr, 2)